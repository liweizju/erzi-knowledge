{
  "name": "erzi-knowledge",
  "version": "0.1.0",
  "description": "二子的知识库 - 跟二子一起学习的交互式知识空间",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-data.js && node scripts/generate-rss.js && node scripts/generate-sitemap.js && vite build",
    "build-data": "node scripts/build-data.js",
    "generate-rss": "node scripts/generate-rss.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "highlight.js": "^11.11.1",
    "marked": "^17.0.1",
    "vue": "^3.5.27"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
